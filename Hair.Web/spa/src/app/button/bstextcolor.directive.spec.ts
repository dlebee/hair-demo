import { BstextcolorDirective } from './bstextcolor.directive';

describe('BstextcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new BstextcolorDirective();
    expect(directive).toBeTruthy();
  });
});
